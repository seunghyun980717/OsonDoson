# Spring API base URL
# 환경별 BASE_URL:
# - Android Emulator (호스트 PC localhost): http://10.0.2.2:8080
# - Android 실기기 + LAN/핫스팟 같은 망: http://<PC LAN IP>:8080
# - tunnel 모드 (다른 망): 백엔드도 별도 노출 필요

# .env.local에 환경별 값 설정 ().env.local은 gitignored)
EXPO_PUBLIC_API_URL=http://10.0.2.2:8080
