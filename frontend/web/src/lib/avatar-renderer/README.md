# Avatar Renderer Module Status

The active signer avatar motion path imports copied `3D/viewer` runtime modules from `src/lib/avatar-viewer`.

This directory still owns shared frontend types, payload normalization, and model loading cache code. The old TypeScript retargeting, rig controller, face strategy, and SEN correction ports are retained only as legacy reference code until the viewer-parity migration is fully verified.
